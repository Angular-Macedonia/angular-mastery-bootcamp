import { ConfirmEmailDirective } from './confirm-email.directive';

describe('ConfirmEmailDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmEmailDirective();
    expect(directive).toBeTruthy();
  });
});
